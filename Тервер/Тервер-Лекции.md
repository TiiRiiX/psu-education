# Теория Вероятности

Чичагов В.В., ауд. 401

chichagovvv@yandex.ru

Учебник - Курс ТВ Борис Владимирович Гляделко

## Вводная часть

**Основное назначение ТВ** - математические модели случайных явлений

**Явление**:

1. Детерминированные
2. Случайные
3. Хаотические

**Детерминированыне** - исход однозначельно определен

**Случайные** - исход однозначено неопределен и наличие свойства статестической устойчивости

### Первичные понятия ТВ

* **Испытание** (эксперимент) - реализация определенного комплекса условий.
* Определенный исход испытания - **событие** (исход) (A, B, C)

```
A = {сдан экзамен}
B = {время в пути не более 10 минут}
```

* Взаимоисключающие друг друга события - **несовместные события**
* **Элементарные события** - наиболее простые несовместные события 
* Множество всех элементарных событий - **пространство элементарных событий** (𝛀)

События:

* **невозможное** - никогда не происходит (∅)
* **достоверное** - всегда происходит (совпадает с пространством элементарных событий)
* **случайное** - событие, которое может произойти, а может и не произойти в результате испытания

**Задача ТВ** - оценить возможность наступления случайного события

**Вероятность** -  числовая характеристика осуществления случайного события `P(A) = 0.8`, `Pr`

## Аксиомотическое определение вероятности

### Операции над событиями:

1. **Пересечение событий** (произведение событий) (∩) (\*):  
   Опр. A1, .., An состоявщее в совместном их проявлении
   Пример:  Aj = {j-й ШБ}
   A1∩A2 = A1*A2 = A1A2

2. **Объеденение событий** (∪) (+) - объеденений событий An состоявщее покрайне мере хотя бы из одного этого события (или A1 или A2 или ... An)

3. **Дополнение событий** (до 𝛀)
   Опр.  A (с чертой вверху) дополнение к событию A, если оно происходит лишь тогда, когда не происходит событие A.
   (объеденение от j=1 до n Aj под A все под чертой) = (пересчение j=1 до n от Aj под чертой) // TODO

   1. **Вложение событий** - A C B - A влечет событие B // TODO

4. **Имплекация событий** A = B, события A и B равносильные называются если, событие A влечет появление события B, а событие B влечет появление события B
   $$
   A ∪ B = Anot(B) + not(A)B + AB
   $$
   // TODO

### Свойства операция ∪ и ∩

1. Коммукативность
   $$
   A ∪ B = B ∪ A
   (A ∪ B) ∪ C = A ∪ B ∪ C
   $$
   // TODO

2. Ассоциативность

3. Взаимная дистрибутивность
   $$
   A ∩ (∪_{j=1}^nB_j) = U_n^{j=1}AB
   $$

4. Принцип двойственности // TODO
   (∩ все под чертой A j=1 n = ∪ только A под чертой)



Ω может быть:

* счетное
* несчетное

Опр. A - 𝜎 - алгебра подмножества из Ω, если обладет следующими тремя свойствами

1. Ω (принадлежит) A // TODO
2. Если a принадлежит A, то а с чертой принадлжет A // TODO
3. Если a1,..,an,... принадлежит A, то объеденение j =1 inf aj принадлежит A // TODO



[a; b) - полуинтервал, из него можно полуит (c, d) [c, d] {c}

Аксиоматическое определение вероятности - Вероятностью (вероятностной мерой) называют числовую функцию `P(.)` заданной на 𝜎 - алгебра событий и удволетворяющим следующим аксиомам:

1. Аксиома неотриацтельности
   P (A) >= 0 для любого A из 𝜎 - алгебра событий
2. Аксиома нормированности
   P(Ω) = 1
3. Аксиома счетной итивности
   Для несовместных событий A1, A2, ..., An,... P(объедениеие их до бесконечноси) = сумма до бесконечности P(Aj) // TODO

(Ω, 𝜎, P) - вероятностное пространство (вероятностная модель)

(Ω, 𝜎) - измеримое пространнство, P - распределение

Пример1

Пусть  
$$
Ω = {ω1, ω2, ...}
// TODO
P(ωj) = pj >= 0, p1 + p2 + ... = 1

P(A) = \sum_{ω_j с A}(Pω_j)
$$
Ω = R

P([a, b)) = F(b) - F(a), где F(x) -  функция распределение случайной величины

**Свойства**

1. Формула сложения вероятностей для несовместных событий
   A1, ..., An
   P(A1 + ... + An) = P(A1) + ... + P(An)

   Опр. A1, ..., An образуют **полную группу событий**, если в результате испытания обязательно произойдет одно из этих событий (объеденение = Ω) (Ai ∩ Aj = ∅)

2. Сумма вероятностей событий образующих полную группу = 1
   P(Ω) = 1 = P(сумма Aj) = сумма (P(Aj)) // TODO

3. P(not(A)) = 1 - P(A) // TODO

4. P(∅) = 0

5. Если A C B,  то P(A) <= P(B)
   P(B) = P(A + not(A)B) = P(A) + P(not(A)B) >= P(A)

6. Для двух совместных событий:
   P(A ∪ B) = P(A) + P(B) - P(AB)

7. 
   $$
   P(∪_{j=1}^nA_j) = 1 - P (∩_{j=1}^nnot(A_j)) = \sum^nP(A_j) + \sum_{i < j}^nP(A_jA_i) + \sum_{i < j < k}^nP(A_jA_iA_k) // TODO
   $$





## Понятие условной вероятности. Независимость событий

Испытание (B)

Дополнительная информация относительно B: произошло событие B
$$
P(A | B)  = P(AB) / P(B) = mab / mb // TODO
$$


Пример вычисления условной вероятности

Имеется сосуд, в нем 4 белых шаров и 6 черных. Начали доставать шарики по одному.

Aj = {j-ый окажется белым}

P(A1) = 4/ 10; P(A2 | A1) = 4 - 1/ 10 - 1

P(not(A4) | A1not(A2)A3) = 5 / 7

P (A2 | A1) = P(A1A2) / P(A1) = (C^2_4/C^2_10)/ (4/10) = (4\*3\*2\*10) / (2\*10\*9\*4) = 3/9

// TODO

Опр:

Появляется при совместимости  P (A | B) и P (A)

**Независимость событий** - P(A | B) = P(A), P(B | A)  = P(B)



*Определение*:
События А, ..., Ak  называются **независимыми совокупностями**, если для любого подмножества этих событий вероятность их пересечени равно произведению вероятностей отдельных событий

Формулы умножения вероятностей:



```
P (A | B) = P(AB) / P(B)

P(AB) = P(B) * P(A|B) = P(A) * P(B | A)

P((AB)C) = P(AB) * P(C | AB) = P(A) * P(B | A) * P(C | AB) 

```

Если A1,..,An - независимое, то `P(A1,..,An) = P(A1) * ... * P(An)`

*Определение*:
Испытание называется **независимым**, если исход одного из них или любого другого, на исход остальных

>  Исход незавсимых испытаний всегда считаются независимыми



## Формула полной вероятности

**Условия**: известно, что событие A может произойти совместно только с одним из событий H1,..., Hk образующие полную группу

`H1,..., Hk` - **гипотизы** 

P(H1),...,P(Hk), P(A | H1),..., P(A | Hk) - оприори известные вероятности

P(A) = sum(j=1, k, P(Hj) * P(A | Hj))

A = AH1 + ... + AHk

*Пример*:

Урна 1: 4 белых + 6 черных, достают 2 штуки во второю урну

Урна 2: 4 белых + 6 черных, достают 1 штуку

A = {шар их 2-ой урны оказывается белым}

H1 = {из 1-ой перекладывают 2 белых}

H2  = {из 1-ой перекладывают 1 белый и 1 черный}

H3 = {из 1-ой перекладывают 2 черных}

P(H1) = C^2_4 / C^2/10 = 6/45

P(H2) = C^4_6 / C^2/10 = 24/45

P(H3) = C^2_6 / C^2/10 = 15/45

P(A | H1) = 6/12, P(A | H2) = 5/12, P(A | H3)  = 4/12

P(A) = 6/45 * 6/12 + 24/45 * 5/12 + 15/45 * 4/12

## Формула Байеса (Бейес) 

*Условия*: 

* известно, что событие A может произойти совместно только с одним из событий H1,..., Hk образующие полную группу
* событие A произошло в результате испытаний

Формула Байеса позволяет вычислить апостариорные вероятности гипотиз  P(Hj | A)

P(H1 | A), ..., P(Hk | A)

`P(Hi | A) = P(Hi) * (A | Hi) / P(A) = P(Hi) * P(A | Hi)/sum(j=1,k,P(Hj) * P(A | Hj))` - **формула Байеса**

*Продолжение примера*:

Шар извлеченный из второй урны оказался белым

Какова вероятность, что из первой урны будет переложено два белых шара?

P (H1 | A)  = 6/45 * 6/12 /  P(A)

 ## Схема независимых испытаний Бернули. Формула Бернули

Описание схемы:

Проводится n-независимых повторых испытаний с двумя противоположенными исходами `A` или `not(A)`

`P(A) = p, A(not(A)) = 1 - p = q`

Такого рода испытания называют испытаниями Бернули.

Появление A называют удача (успех), появление not(A) - неудача

`A = {удача, успех}, not(A) = {неудача}`

*Задача №1*

>  Найти P_n(m) того ровно `l` испытаний из `m` окажутся успешными

`P_n(m) = C^m_n * p^m * q^(n-m), m = 0,1,...,n`

*Задача №2*

> Найти Pn(m1, m2) число успехов в n испытаниях в пределах [m1, m2]

`Pn(m1, m2) = Pn(m1) + Pn(m1 + 1) + ... + Pn(m2)`

*Задача №3*

>  Определение наивероятнейшего числа успехов (m0)

`np - q <= m0 <= np + p` - не более двух целых решений

## Приближенные вычисления в результатах Бернули

n -> inf, n >= 50 (лучше n >= 100)

n! можно оценить с помощью Стирлинга

n! ~ sqrt(2Pi\*n) * (n/e)^n

Для приведение приближенных вычислений используется теорема Паулсона и ее следствия или глобальная и интегральная теорема Муавр-Лапласа

> Эврестическая рекомендация
> Если n\*p\*q >= 9, 1/(n+1) < p < n/(n+1) => то Муавр-Лаплас, иначае Паулсон



## Теорема Пуассона

По схеме испытаний Бернулли число испытаний n -> inf, p -> 0, n*p -> lambda > 0 // TODO, тогда

lim(n -> inf, Pn(m)) = (lamda ^ m)/(m!) * e ^ (-lambda), m=0,1,...

Данный результат используется для оценки редкий событий.

lambda = Среднее число редких событий происходящей за некоторый промежуток времени на некоторой территории

**Следствие**

lim(n -> inf, Pn(m1, m2)) = sum(m=m1, m2, (lambda^m / m!) * e ^ (-lambda))

При практических lambda = n*p и предел заменяют на примерно равно

Введем обозначения

фи_маленькое(x) = 1/sqrt(2Pi) * e^(-x^2/2) - плотность - распределение нормальной случайной величины // TODO

Ф(x) = integrate(-inf, x, фи_мал(t)dt) - функция распределения стандартной случайной величины

Свойства фи_мал и  Ф

1. при |x| >= 4.0 фи_мал(x) = 0.0000
2. фи_мал(0) = 1/sqrt(2Pi) = 0.4
3. Ф(+inf) = 1, Ф(-inf) = 0
4. Ф(x) - возрастающая
5. Ф(-x) = 1 - Ф(x)
   Ф(0) = 1/2
6. при x>=4, Ф(x) = 1,0000

## Интегральная теорема Лаплпаса

Пусть в схеме независимых повторных испытаний Бернулли число испытаний неогранничено возрастает, при этом вероятность остается величиной постоянной 0 < p < 1

`|(mi - np)/sqrt(npq)| <= C, i = 1,2`, то

`Pn(m1;m2) ~= Ф((m2 - np)/sqrt(npq)) - Ф((m1 - np)/sqrt(npq))`

Следствие

`P( |(m(delta)/n) - p | <= eps) ~= 2Ф(eps*sqrt(n/(pq))) - 1`

P (np - n\*eps <= m(A) <= np + n\*eps) ~= Ф(eps\*sqrt(n/(pq)) - Ф(-eps\*sqrt(n/(pq)) = {Ф(-x) = 1 - Ф(x)} = 2Ф(eps\*sqrt(n/(pq)))

Замечание

P(m(A) <= m) = Ф((m+0.5 - np)/sqrt(npq))

## Случайные величины и их числовые и вероятностные характеристики

Пусть испытание заключается в наблюдении числового показателя, имеющего случайную природу

Такой случайный показатель называют **случайной величиной**

Пусть (Ω, A, P) (R, B_r, P_кси)

Опр

Числовую функцию кси = кси(омега) опр на Ω,называют случайной величиной, или A-измеримой функцией, если для любого действительного значения X, на которых {кси(омега) - x} принадлежит A

Пример

Бросают кубик

омега 1 2 3 4 5 6

кси(омега) 1 2 3 -1 -2 -3

| омега      | 1    | 2    | 3    | 4    | 5    | 6    |
| ---------- | ---- | ---- | ---- | ---- | ---- | ---- |
| кси(омега) | 1    | 2    | 3    | -1   | -2   | -3   |

P_кси(B) = P(кси попадает в B) = P ({омега : кси(омега) попадает в B})

С помощью закона распределения вероятностей

Случайная величина:

* Дискретная - если СВ может принимать не более счетное счисла значений, каждое из этих значений может наблюдаться с некоторой положительной вероятностью
  * Число успехов в n-независимых испытаниях
  * Число пользователей в интернете
* Непрерывная
  * Масса вещества в каком-то объеме

Опр

`F_кси(x) = P (кси_б < x)` - доля распределения сосредоточенная левее точки `x`

Пример

кси - доход гражданина РФ (рубл)

F_кси(15000) = 0.2

`F_кси(x) = P (кси < x)`

1. 0 <= F_кси(x) <= 1
2. x1 < x2 => {кси < x1} {кси < x2}
F_кси(x1) = P(кси < x1) <= P(кси < x2) = F_кси(x2)
3. Являяется непрерывной слева функцией
4. F_кси(+inf) = 1 F_кси(-inf) = 0
5. P(a <= кси < b) = F_кси(b) - F_кси(a)
P(a<= кси <= b) = F_кси(b + 0) - F_кси(a)

{ кси <= b} = {кси < a} + {a <= кси < b>}

6.  

Т. Лебега

F_кси(x) = W1F(x) + W2F(x) + W3F(x)

Wi >= 0, W1 + W2 + W3 = 1

F_дискретн(X), F_непрерыв(X), F_сигнул(X)

Множество точек ее роста имеет нулевую меру (длинну)

## Анализ распределения дискретного типа

X - случайная величина, которая может принимать значения x1, x2,..., xn

Следовательно введем p1,p2,...,pn

X ~ {(xi, pi), i=1,n}

|X|x1||xn|
|-|-|-|-|
|Px|p1||pn|

{pj} - ряд распределения вероятности

p1 + ... + pn = 1

{X = x1}, ... , {X = xn} - полная группа событий

### Типовые задачи

1. Нахождения закона распределения дискретной случайной величины

*Пример*

Функционируют два банка, которые могут развалится в течении года независимо с вероятностями 0.1 и 0.05

Найти ЗР X равной числу банков разоривщихся в течении года

*Решение*

|X|0|1|2|
|-|-|-|-|
|Px|0.855|0.14|0.005|

Aj = {j-ый банк разорится}, j = 1,2

P(A1) = 0.1 P(A2) = 0.05

P(X = 0) = P(not(A1)not(A2)) = 0.9 * 0.95 = 0.855

P(X = 2) = P(A1A2) = 0.1 * 0.05 = 0.005

P(X = 1) = 1 - P(X = 0) - P(X = 2)

2. Вычисление вероятности X в заданную область B

P(X принадлежит B) = P(sum{x1 принадлежит B, X = X1}) = sum{i: x1 прин. B, pi}

*Продолжение примера*

P(1 <= X < 2.1) = P(X = 1) + P(X = 2) = 0.14 + 0.005 = 0.145

P(X > 2.5) = 0

3. Нахождение суммы функции распределения случайной величины

F_X(x) = P(X < x)

F_x(x) = sum{i: xi < x , pi}

*Особое свойство функции распределения дискретной случайной выличины*

1. Является ступенчетой функцией с точками разрыва x1, x2,...,xn
2. При переходе через точку разрыва xj, функция получает приращение pj

Докажем

x1 < x2 < ... < xn

1. x_j-1 < x < xj

FX(x) = sum {i: 1, j - 1, pj}

2. FX(xj) = P(X < xj) = sum{i = 1, j - 1, pi}

FX(xj + 0) = P(X < xj + 0) = sum{i = 1, j, pj}

*Продолжение примера*

условное выражение - 

FX(x) = {

  0, x <= 0

  0.855, 0 < x <= 1

  0.995, 1 < x <= 2

  1, x > 2

}

FX(2) = P(x < 2) = 0.995

4. Нахождение числовых характеристик

ЧИХ СВ - некое число содержащее общую информацию об распределении случайной величины

1. Математическое ожидание M[X], E[X]

*Определение*

X - дискретная случаная величина заданная набором значений X ~ {(xi, pi), i=1,n}

Тогда

`M[X] = sum{xi, pi}`


М.О. характеристика положения центра распределения случайной величины

Математиматическое ожидание - характеристика экономической эффективности некой операции

Примечание

Для любой измеримой функции

`M[h(x)] = sum {h(xi)*pi}`

2. Дисперсия `D[X], Var[X], V[X]`

*Определние*

`D[X] = M[X - M(x)] ^ 2`

Явлется характеристикой изменчивости случайной величины

В экономических приложениях дисперсия используется для меры риска совершаемой экономической операции.

Формула для вычисления дисперсии

`D[X] = sum{(xi - h(xi))^2 * pi}`

3. Средне-квардратичное отклонение сигма(x)

*Определение*

`сигма(x) = sqrt(D(x))`

4. Начальный момент k-ого порядка `M_k`

*Определение*

`mk = M[X^k], k = 0,1,2...`

`mk = sum{xi^k * pi}`

5. Центральный момент k-ого порядка `мю_k`

`мю_k = M[X - M(x)] ^ k`

`мю_k = sum {(xi - M(X)) ^ k *pi}`

Примичание

Дисперсию можно вычислить через моменты

`D[X] = m2 - m1^2 = M[X^2] - M^2[X]`

6. Медиана дискретной случайной величины `x_0.5  med(X)`

*Определение*

X_0.5 - медиана случайной величины X, если P(X<=x_0.5) >= 0.5 P(X >= x_0.5) >= 0.5

*Пример*

X - зп гражданина некоторого региона

|X|10|200|
|-|-|-|
|Px|0.90|0.10|


M[X] = 10 * 0.9 + 200 * 0.10 = 29

med = 10

7. Мода распредиления (moda(X))

Мода распределение - наивероятнешее значение случаной величины (их может быть несколько, если им соотвестует одно значение вероятности)

5. Нахождение распределение функции от случайной величины

*Постановка*

X ~ {(xi*pi), i=1,n}

h(X) =  y

Найти закон распределения(D) y

h(x1), ..., h(xn)

Y - распределение случайной величины

y1, ..., ym (m <= n)

P(Y = yj) = P(sum{i: h(xi) = yj, X = xi}) = sum{i: h(xi) = yj, pi}

*Продолжение примера*

Найти закон распределения Y = |X - 1|

|X|0|1|2|
|-|-|-|-|
|Y=\|X-1\||1|0|1|

|Y|0|1|
|-|-|-|
|Py|0.14|0.85|

P(Y = 0) = P(X = 1) = 0.14

P(Y = 1) = P(X = 0) + P(X = 2) = 0.86

## Наиболее известное распределения дискретных случайных величин

1. Биномиальное распределение B(n;p)

*Определение*

Проводится n независимых испытаний исходом каждого из событий может быть A или not(A). P(A) = p, P(not(A)) = q

Тогда X = числу появлению A (число успехов) 

P(X = m) = C^m_n\*p^m\*q^(n-m), m=0,1,2...,n

M[X] = np

D[X] = npq

*Опр*

Производящее функции начальных моментов называются кси(z) = M[e^(xZ)], где X - заданная случайная величина, z пренадлежит R

Свойство: производного k-ого порядка в нуле совпадает с M[X^k]
```
кси(z) = M[e^(zX)] = sum{e^(zm)C^m_n*p^m*q^(n-m)} = {sum{C^m_n*a^m*b^(n-m)} = (a+b)^n} = sum{C^m_n*(p*e^z)^m*q^(n-m) = (p*e^z + q)^n}
```

кси'(z) = n(p*e^z + q)^(n-1); кси'(0) = np

кси''(z) = n(n - 1)(pe^z+q)^(k-2)*(pe^z)^2+n(pe^z+q)^(n-1)pe^z

кси''(0) = M[X^2]

D[X] = n(n-1)p^2 + np - (np)^2 = np - np^2 = npq

2. Распределение Поуссона

П(лямда)

*Опр*

X ~ П(лямбда), лямбда > 0, если ее распределение задается с помощью формулы Поуссона.

P(X = m) = (лямбда^m/m!)e^(-лямбда), m=0,1,2...

M[X] = лямбда

D[X] = лямбда
```
M[X] = sum{m = 1, inf, m*(лямбда^m/m!)e^(-лямбда)} = sum{m = 1, inf, (лямбда^m/(m-1))*e^(-лямбда)} = {j = m-1, m = j +1} = sum{j =0,inf, (лябмда^(j+1)/j!)*e^(-лямбда)} = лямбда* sum{(лябмда^j/j!)/e^(-j)} = лямбда

M[X^2] = sum{m = 1, inf, m^2*(лямбда^m/m!)e^(-лямбда)}, m = m(m-1)+m = sum{m(m-1)*(лямбда^m/m!)*e^(-лямбда)} + sum{m*(лямбда^m/m!)*e^(-лямбда)}={j = m - 2, m = j + 2} = sum{(лямбда^(j+2)/j!)*e^(-лямбда)} + лямбда = лямбда ^ 2 + лямбда
```

Распределение Поуссона - Одна из моделей числа редких событий происходящих на определенной территории за определенный промежуток времени

3. Отрицательное-биоминальное распределение NB(k, p)

Возникает в схеме независимых повторых испытаний с двумя исходами (успех, неудача) осуществляемых до k-ого успеха

p - вероятность успеха

q = 1 - p

X ~ NB(k, p)
```
P(X = m) = C^(k-1)_(m+k-1)*p*q^m ; m=0,1,2,3...

sum{C^(k-1)_(m+k-1)*p*q^m} = 1

sum{C^(k-1)_(m+k-1)(k*p^(k-1)*(-1)+nq^(m-1))} = 0

sum{(m/q - k/p) * C^(k-1)_(m+k-1)*p^k*q^m} = 0

M[X]/q = k/p * 1

m[X] = kq/p
```

4. Гимердиметрическое распределение H(N, D, n)

Возникает в схеме выборки без возвращения

Пусть N количество различных элементов, из них D элементов первого типа, N - В - второго типа

Из исходной совокупности извлекается случайная выборка n

Тогда X выборки первого типа имеет гипердиметрическое распределение задаваемого формулой

```
P(X = m) = (C^m_D*C^(n-m)_(N-D)/C^n_N), m = 0,1...,min(n, D)

M[X] = n*(D/N)

D[X] = n*(D/N)*(1 - D/N) * ((N - n)/(N - 1))
```

## Случайные величины неприрывного типа

*Опр*

Случайная величина кси являеется случайной величиной неприрывного типа (имеет абсолютно неприрывную функцию распределения), если ее распределение вероятности может быть задана с помощью функции плотности этого распределения.

Плотностью распределения вероятностей (плотность) будем называть числовую функцию, которая удволетворяет следующему f_кси(x) integrate(f_кси(x)dx) = F_кси(x)

Свойства плотности:

1. Удволетворяет условию нормировки integral(-inf, +inf, f_кси(x)dx) = 1 (=F_кси(+inf))
2. В точке f_кси(x) = F_кси'(x)
3. f_кси(x)>=0, где существует x

f_кси(x) = дельта F_кси(x)/ дельта x = F_кси(x + (дельта x)/2) - F_кси(x - (дельта x)/2)/дельта x = (P(x - (дельта x)/2) <= кси <= x + (дельта x)/2)/дельта x

Свойства распределения непрерывного типа:

1. Если кси имеет плотность, то F_кси(x) является непрерывной функцией
2. P(x1 < кси < x2) = P(x1 <= кси <= x2) = integral_x1^x2(f_кси(x)dx)
3. P(кси = x) = 0
4. Вероятность попадания кси в область B пренадлежащей R P(кси пренадлежит B) = integral_B(f_кси(x)dx)

m кси = sum{xi*pi}

Определение

Если кси ~ f_кси(x), то m кси = integral(-inf, +inf, x*f_кси(x)dx)

Mh(кси) = integral(-inf, +inf, h(x)*f_кси(x)dx)

2. D, mk, галочка

D = integrate((x -mкси)^2*f(x))

3. Квантиль уровня p

*Опр.* Квантилем уровня p распределения случайной велечины кси ~ F(x) x_p являющееся решением уравнения 

F(x) = p, 0 < p < 1

Квантиль опеределяет точку левее которой собрано большая часть распределения

* x_0.5 - медиана распределения
* x_0.25 - нижняя квантиль
* x_0.75 - верхняя квартиль

x_0.75 - x_0.25 - интерквантильный размах. Применяется, когда дисперсия не существует

4. Мода распределения moda(кси)

*Опр* Точка локального максимума функции плотности

Если одна точка - одомодальной, если несколько - мультимодальная(полимодальная)

### Характеристики унимодального распределения:

5. Коофициент асиммитрии

*Опр* нормированный центральный момент третьего порядка

k1 = галочка_3/круглаяштука_3^3

6. Коофициент эксцесса (эксцесс): `k2`

*Опр* k2 = ню(или мю)_4/сигма_кси^4 - 3

Характерезует кривизну вершины

### Распространенные распределения непрерывного типа:

1. Равномерное распределение на (a; b) `U(a; b)`

кси ~ U(a; b), если f_кси(x) = {
  C, x принадлежит [a; b], 
  0, x не принадлежит [a; b]
}

Вероятностные характеристики распределения:

F_кси(x) = integral(-inf, x, f_кси(x)) = {
  0, x<=a,
  integral(a, x, 1/b-a) = (x-a)/(b-a), a < x <= b,
  1, x > b
}

Если a < x <= b, то (x-a)/(b-a) = p(кси <= x)

М_кси = (a+b)/2 = integral(xf_кси(x)) = integral(a, b, x*1/(b-a))

D_кси = (b-a)^2/12

2. Экспонинцальное распредление `E(лямбда)`

*Опр* кси ~ E(лямбда), лямбда > 0

`f_кси(x) = лябмда*e^(-лябмда*ч), x >= 0`

* Используется в качестве простейшей модели продолжительность какой-либо технической операции
* Продолжительность работы изделия до отказа
* Продолжительность жизни технического изделия

Гамма-функция: Г(a)

*Опр* Г(a) = integral(0, inf, t^(a-1)*e^(-t)), a>0

1. a - натуральное число -> Г(a) = (a-1)!
2. Рекуретная формула: Г(a) = (a - 1)Г(a - 1)
3. Г(1/2) = sqrt(Pi)

Числовые характеристики показательного распределения:

м_кси = 1/ламбда, D_кси = 1/лямбда^2

лямбда = 1/м_кси
```
m_k = М*кси^k = integral(-inf, +inf, x^k*f_кси(x)) = integral(0, +inf, x^k*лямда*e^(-лямбда*x)) = {лямда*x = t, x = t/лямда, dx = dt/лямбда} = integral(0, +inf, (t/лямбда)^k*e^(-t)) = 1/лямбда^k * integral(0, inf, t^k*e^(-t)) = k!/лямбда^k
```

`При x>=0: F_кси(x) = integral(0, x, лямбда*e^(-лямбда*x)) =  1 - e^(-лямбда*x)`

P(кси >= x) = 1 - F_кси(x) = e^(-лямбда*x)

3. Нормальное распределение N (Гаусовское)

N(a, сигма^2)

*Опр* кси ~ N(a, сигма^2) с m*кси=a, D_кси = сигма^2, если плотность распределения задается 

f_кси(x) = 1/(сигма*sqrt(2Pi)) * exp{-(x-a)^2/(2сигма^2)}, x принадлежит R

Случайная ошибка модели; Sn = sum(кси_k)

При a = 0, сигма = 1 нормальное распределение называют стандартным распределнием. кси имееет стандартная нормальное распределение.

f(x) = 1/sqrt(2Pi) * e^(-x^2/2) = фи(x)

x = a - moda, медиана, уномодальная, М.О.

Точки перегиба - a +- сигма

f_кси(a) = 1/(сигма*sqrt(2Pi)) = 0.4/сигма

f_кси(a+-сигма) = 1/(сигма*sqrt(2Pi)) * e^(-.0.5) =~ 0.24/сигма

Числовые характеристики

M = a; D = сигма^2

Функция распределения и нормальные вероятности:
```
F_кси(x) = integral(-inf, 0, 1/(сигма*sqrt(2Pi)) * exp{-(x-a)^2/(2сигма^2)}) = {(x-a)/сигма = t, dx/сигма = dt} = integral(-inf, (x-a)/сигма, 1/sqrt(2Pi)*e^(-t^2/2)) = Ф((x-a)/сигма)
```

F_кси(x) = Ф((x-a)/сигма)

1. P(x1 <= кси <= x2) = F_кси(x2) - F_кси(x1) = Ф((x2-a)/сигма) - Ф((x1-a)/сигма)
2. P(|кси - a| <= дельта) = 2Ф(дельта/сигма) - 1
3. P(a - дельта <= кси < a + дельта) = Ф(дельта/сигма) - Ф(-дельта/сигма) = 2Ф(дельта/сигма) - 1
